# Streamlit's hard ceiling for a single uploaded file (MB). Set high so the real,
# tunable limit is the app's UPLOAD_MAX_MB (.env, default 200) — Streamlit only
# needs to allow at least that. Raise this too if you push UPLOAD_MAX_MB above it.
[server]
maxUploadSize = 1024
